# Jira Scraper Requirements
# Python 3.10 - 3.13 compatible

# Jira API Client
# Note: Use atlassian-python-api instead of jira for Python 3.13 compatibility
# The 'jira' package has issues with Python 3.13 (pkgutil.ImpImporter removed)
atlassian-python-api>=3.41.0

# Data manipulation
polars>=0.20.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.14.0

# Utilities
python-dateutil>=2.8.0
requests>=2.31.0
python-dotenv>=1.0.0

# Optional: If you still want to try the jira package with Python 3.13
# You would need to downgrade to Python 3.11 or wait for jira package update
# jira>=3.5.0
